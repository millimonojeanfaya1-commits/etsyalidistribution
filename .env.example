# Example environment variables for GestionStock
# Copy this file to .env and adjust values for your environment.

# Django settings
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=change-me-to-a-strong-secret-key
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Database (defaults to SQLite if not configured in settings.py)
# DATABASE_URL=postgres://user:password@localhost:5432/gestionstock

# Timezone and Locale (optional)
DJANGO_TIME_ZONE=Africa/Kinshasa
DJANGO_LANGUAGE_CODE=fr-fr
